/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com','www.tonoto.in'], // Allow Cloudinary image URLs
    },
  };
  
  export default nextConfig;
  